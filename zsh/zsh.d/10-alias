# vim: set ft=zsh :

if [[ "$(uname)" == "Linux" ]]
then
  alias ls='ls --color=auto'
else
  alias ls='ls -hFG'
fi
alias ll='ls -lh'
alias df='df -h'
alias du='du -h'

function gr {
  if [ -x "gradlew" ]; then
    ./gradlew $@
  else
    gradle $@
  fi
}
