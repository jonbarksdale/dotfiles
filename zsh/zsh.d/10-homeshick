# vim: set ft=zsh :
#
HOMESHICK_HOME="${HOME}/.homesick/repos/homeshick"


if [[ -d "$HOMESHICK_HOME" ]]
then
    source "$HOMEHICK_HOME/homeshick.sh"
    fpath=($HOMESHICK_HOME/completions $fpath)
fi

