# vim: set ft=zsh :
#
export HOMESHICK_HOME="${HOME}/.homesick/repos/homeshick"


if [[ -d "$HOMESHICK_HOME" ]]
then
    source "$HOMESHICK_HOME/homeshick.sh"
    fpath=($HOMESHICK_HOME/completions $fpath)
fi
